import setuptools

setuptools.setup(
    name="mpl_resize",
    version="0.1.0",
    author="Suraj Gowda",
    description="Resizing matplotlib subplots to optimally fit a canvas",
    packages=setuptools.find_packages(),
    classifiers = [
        "Programming Language :: Python :: 3"
    ],
)
